package by.it.plugatar.calc;

import java.io.PrintWriter;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.Map;

/*
public class VarFile {
    private static String varFile= getPath("vars.txt");


    static  void load(){
        try{
            Parser parser = new Parser();
            Files.lines(Paths.get(varFile));
        }
    }
*/
    //static String getPath из HElper
 /*   static void save(Map<String,Var> vars){
        try {
            try (PrintWriter writer = new PrintWriter(varFile){
                for (Map.Entry(String,Var) entry :vars.entrySet()){
                    writer.printf("%s=%s",)
                }

            }
        }


    }
}
 */